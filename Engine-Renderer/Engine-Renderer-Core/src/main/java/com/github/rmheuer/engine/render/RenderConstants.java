package com.github.rmheuer.engine.render;

public final class RenderConstants {
    public static final int MAX_TEXTURE_SLOTS = 16;
    public static final int MAX_OBJECTS_FREED_PER_FRAME = 64;

    private RenderConstants() {
        throw new AssertionError();
    }
}
